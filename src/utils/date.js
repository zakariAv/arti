

export const years = () => {
    const years = [];
    for(let i =1930 ; i <= 2024 ; i++){
     years.push(i)
    }
    return years
 }

export const months = () => {
   const months = [];
   for(let i= 1 ; i<=12 ; i++){
     months.push(i)
   }
   return months
 }

 export const days = () => {
   const days = [];
   for(let i= 1 ; i<=31 ; i++){
     days.push(i)
   }
   return days
 }
